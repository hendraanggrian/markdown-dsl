package com.hendraanggrian.markdown

import org.junit.Test

class BlockquoteBuilder {

    @Test
    fun blockquoteLine() {
        /*assertEquals(
            buildMarkdown {
                blockquote("Hello world")
            }.content.trim(),
            ">Hello world"
        )
        assertEquals(
            buildMarkdown(true) {
                blockquote("Hello world")
            }.content.trim(),
            "> Hello world"
        )*/
    }
}